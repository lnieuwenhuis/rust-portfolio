[build]
builder = "RAILPACK"
buildCommand = "cargo build --release"

[deploy]
startCommand = "./target/release/lars-portfolio"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
