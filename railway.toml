[build]
builder = "RAILPACK"
buildCommand = "cargo build --release && mkdir -p bin && cp target/release/lars-portfolio bin/lars-portfolio"

[deploy]
startCommand = "./bin/lars-portfolio"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
